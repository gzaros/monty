#include "monty.h"

/**
 * _nop - do nothing
 * @row_cnt : nothing
 * @stack: it does not matter
 *
 * Return: do (nothing)
 */

void _nop(stack_t **stack, unsigned int row_cnt)
{

	(void) row_cnt;

	(void) stack;
	
}
